# Next.js 16 Latest Patterns Update

## TL;DR

> **Quick Summary**: Update app metadata from placeholder to actual app info. Codebase is already fully aligned with Next.js 16 patterns.
> 
> **Deliverables**:
> - Updated metadata in `layout.tsx`
> 
> **Estimated Effort**: Quick (< 15 minutes)
> **Parallel Execution**: NO - single task
> **Critical Path**: Task 1 only

---

## Context

### Original Request
Update Next.js project to use latest patterns according to latest documentation for all packages.

### Interview Summary
**Key Discussions**:
- Cache Components/PPR: User chose to SKIP - not needed for redirect app
- Metadata update: User approved - change to "Link-To-Me - YouTube Redirect Service"

**Research Findings**:
- Route handlers already use `params: Promise` pattern (Next.js 15+ requirement)
- Already using `await headers()` for async request APIs
- Tailwind CSS 4 patterns already in place (`@import "tailwindcss"`, `@theme`)
- ESLint flat config already compatible with Next.js 16
- TypeScript config already uses `moduleResolution: "bundler"`
- Turbopack already enabled via `pnpm dev --turbopack`

### Current State Analysis

| Area | Status | Notes |
|------|--------|-------|
| `src/app/[...path]/route.ts` | ✅ Correct | `params: Promise`, `await headers()` |
| `src/app/redirect/page.tsx` | ✅ Correct | Suspense boundary with useSearchParams |
| `src/app/globals.css` | ✅ Correct | Tailwind v4 CSS-first pattern |
| `eslint.config.mjs` | ✅ Correct | ESLint 9 flat config |
| `tsconfig.json` | ✅ Correct | `moduleResolution: "bundler"` |
| `next.config.ts` | ✅ Minimal | No changes needed (no custom loaders) |
| `src/app/layout.tsx` | ⚠️ Update needed | Placeholder metadata |

---

## Work Objectives

### Core Objective
Update the root layout metadata from placeholder "Create Next App" text to actual app information.

### Concrete Deliverables
- Updated `src/app/layout.tsx` with correct title and description

### Definition of Done
- [ ] `pnpm build` completes successfully
- [ ] `pnpm lint` passes with no errors
- [ ] Metadata reflects "Link-To-Me" branding

### Must Have
- Title: "Link-To-Me - YouTube Redirect Service"
- Description: Reflects app purpose (YouTube link redirection)

### Must NOT Have (Guardrails)
- NO changes to routing logic
- NO new dependencies
- NO experimental flags (user declined cacheComponents)
- NO changes to existing async patterns (already correct)

---

## Verification Strategy

### Test Decision
- **Infrastructure exists**: NO
- **User wants tests**: Manual-only (quick task)
- **Framework**: none

### Automated Verification

Each task uses build commands for verification:

```bash
# Build verification
pnpm build

# Lint verification  
pnpm lint

# Type check
npx tsc --noEmit
```

---

## Execution Strategy

### Single Task (No Parallelization Needed)

This is a minimal update - single task, no dependencies.

```
Task 1: Update layout.tsx metadata
   └── No dependencies, starts immediately
```

### Agent Dispatch

| Wave | Tasks | Recommended Agents |
|------|-------|-------------------|
| 1 | 1 | `category="quick"` - trivial file edit |

---

## TODOs

- [ ] 1. Update layout.tsx metadata

  **What to do**:
  - Change `title` from "Create Next App" to "Link-To-Me - YouTube Redirect Service"
  - Change `description` from "Generated by create next app" to a meaningful description like "YouTube link redirection service - automatically opens videos in the YouTube app on mobile devices"

  **Must NOT do**:
  - Do NOT add any new imports
  - Do NOT modify the font configuration
  - Do NOT change the RootLayout component structure
  - Do NOT add experimental flags or features

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Single file edit, < 5 lines changed, trivial complexity
  - **Skills**: None required
    - No special skills needed for simple metadata update
  - **Skills Evaluated but Omitted**:
    - `frontend-ui-ux`: Not needed - no visual/design changes

  **Parallelization**:
  - **Can Run In Parallel**: N/A (only task)
  - **Parallel Group**: N/A
  - **Blocks**: None
  - **Blocked By**: None

  **References**:

  **Pattern References**:
  - `src/app/layout.tsx:15-18` - Current metadata export location to modify

  **Documentation References**:
  - `README.md` - App description: "YouTube 링크 리다이렉션 서비스"
  - `AGENTS.md:1-5` - Project description for context

  **WHY Each Reference Matters**:
  - `layout.tsx:15-18`: Exact location of the Metadata export that needs updating
  - `README.md`: Source of truth for app branding and description text

  **Acceptance Criteria**:

  **Automated Verification (agent-executable):**
  ```bash
  # 1. Build succeeds
  pnpm build
  # Assert: Exit code 0, no errors

  # 2. Lint passes
  pnpm lint
  # Assert: Exit code 0, no errors

  # 3. Type check passes
  npx tsc --noEmit
  # Assert: Exit code 0, no type errors

  # 4. Verify metadata content
  grep -q "Link-To-Me" src/app/layout.tsx
  # Assert: Exit code 0 (string found)
  
  grep -q "YouTube" src/app/layout.tsx
  # Assert: Exit code 0 (string found)
  ```

  **Evidence to Capture:**
  - [ ] Terminal output from `pnpm build`
  - [ ] Terminal output from `pnpm lint`

  **Commit**: YES
  - Message: `chore(metadata): update app title and description from placeholder`
  - Files: `src/app/layout.tsx`
  - Pre-commit: `pnpm lint && pnpm build`

---

## Commit Strategy

| After Task | Message | Files | Verification |
|------------|---------|-------|--------------|
| 1 | `chore(metadata): update app title and description from placeholder` | src/app/layout.tsx | pnpm lint && pnpm build |

---

## Success Criteria

### Verification Commands
```bash
pnpm build        # Expected: Build successful
pnpm lint         # Expected: No errors
npx tsc --noEmit  # Expected: No type errors
```

### Final Checklist
- [ ] Title is "Link-To-Me - YouTube Redirect Service"
- [ ] Description reflects YouTube redirect functionality
- [ ] No other files modified
- [ ] Build passes
- [ ] Lint passes
